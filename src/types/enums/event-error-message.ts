enum EventErrorMessage {
  EVENT_NOT_FOUND = 'Event not found',
  FAILED_TO_FETCH = 'Failed to fetch events',
  FAILED_TO_CREATE = 'Failed to create event',
  FAILED_TO_UPDATE = 'Failed to update event',
}

export { EventErrorMessage }
